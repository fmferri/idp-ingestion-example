apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: nextgen-user-api
  description: CRUD operation on the users
spec:
  type: openapi
  lifecycle: production
  owner: user-relations-team
  system: user-engagement-portal
  definition: 
    $text: 'https://github.com/fmferri/idp-ingestion-example/blob/main/swagger.json'